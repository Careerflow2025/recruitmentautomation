'use client';

import ClientsDataGrid from '@/components/grid/ClientsDataGrid';

export default function ClientsPage() {
  return (
    <div className="h-screen w-screen overflow-hidden">
      <ClientsDataGrid />
    </div>
  );
}
